

function checkItems(shoppingCart, shoppingItem) {
    return shoppingCart.includes(shoppingItem);
}
// console.log(checkItems(['boek']))
// console.log(checkItems(['kitten melk', 'apples']))

//Returns the size of the array: 
function getArraySize(arr) {
return arr.length
    }
    
    console.log(getArraySize(["a", "abc"])); 
    console.log(getArraySize(["Sam", "Alex", "Charlie"])); 
